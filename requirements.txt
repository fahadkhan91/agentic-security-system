watchdog==3.0.0
plyer==2.1.0
requests==2.31.0
streamlit==1.31.0
pyyaml==6.0.1

# For notifications
# Email: Built into Python (no install needed)
# WhatsApp: requests (already listed above)
# Desktop: plyer (already listed above)
